import 'package:flutter/material.dart';
import 'package:location/location.dart';

import '../helpers/locationHelper.dart';

const apiKey = "GDkyA02ZxfGOkoinxIIAAmZ9Ny7lXIgE";

class GetAddress {
  static String? add(String? firstName) {
    print(firstName);
    return firstName;
  } //GENERATING IMAGE USING THE URL where we input our longitude and latitude and api and zoom too
}
